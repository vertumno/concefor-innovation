import { PollScreen } from "@/components/PollScreen";

export const metadata = {
  title: "Enquete ao vivo — VIII Concefor",
  robots: { index: false, follow: false },
};

export default function EnquetePage() {
  return <PollScreen />;
}
